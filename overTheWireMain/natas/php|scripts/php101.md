$Var["xxx"] is dictionary \
$data['bgcolor'] and $data["bgcolor"] are equivalent \
array_key_exists("data", $_COOKIE) checks: "Does the browser's request contain a cookie named data?" \

```
# xor encryption
Plaintext ^ Key = Ciphertext
Ciphertext ^ Plaintext = Key
New_Plaintext ^ Key = New_Ciphertext
```

in php, 
`when you build a string using double quotes "...", any literal double quote inside it must be escaped with a backslash (\").`\
also 
```\

PHP opens with ", typing 'natas18' means MySQL sees username="'natas18'..."
—it treats the single quote as part of the username rather than closing the string.
You need to close it with a double quote: natas18".
```

to commentize something, usually needs a `-- `, a trailing space, but use # can avoid it
payload = f'natas18" AND password LIKE BINARY "{guess}%" AND SLEEP(2) #'\

#### Magic Methods
```
In PHP, functions start with a double underscore __ are called Magic Methods.
like $logger->__destruct() would not appear. PHP executes them automatically behind the scenes:
```
```
__construct(): Runs automatically when an object is first created with new Logger().
__destruct(): Runs automatically by the PHP engine whenever an object is destroyed. In web requests, every object in memory is destroyed when the PHP script finishes executing right before sending the response.
```
